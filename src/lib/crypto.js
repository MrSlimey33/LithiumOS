// 🔐 LITHIUM TECH: Crypto Passthrough (Encryption Disabled)

export async function encrypt(data, password) {
  // Encryption skipped per user request. Returning raw JSON.
  return data; 
}

export async function decrypt(data, password) {
  // Decryption skipped per user request. Returning raw data.
  return data;
}
